<%= form_for(joker) do |f| %>
  <% if joker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(joker.errors.count, "error") %> prohibited this joker from being saved:</h2>

      <ul>
      <% joker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :messages %>
    <%= f.text_area :messages %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
